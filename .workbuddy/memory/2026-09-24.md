# 2026-09-24 工作日志

## 1.43 发布：修「无蓝牙兜底把 P2P 组拆掉」（车机实机日志实锤）
- 用户实测 1.42 直连仍失败，给了 13 份车机会话日志（carlife-logs-3）。
- 20:17 会话根因：兜底挂载 → `armTransport` 反射调 `m/m/b.b(兜底传输)` → b() 对其它传输调 `f()` → `e/d.f()` 停自举器调 `j.b()` → j.b() 内「亿连三连清」(clearLocalServices+removeGroup+stopPeerDiscovery) 在 1.40 keepP2pGroup 闸门**之前**执行 → 刚建好的 DIRECT-cH-CarLife-HU 组被拆（20:17:58 terminate → 20:17:59 groupFormed:false）→ 90s 轮询「直连组未建立」。
- 1.40 realme 测试没炸只是因为测试时组没建起来；车机 P2P 稳定建组才暴露。
- 修：`m/m/d/j.smali` b() 三连清加 `keepP2pGroup()` 闸门（true 只停循环/释放组播锁）。versionCode 143 / mod1.43。
- 成品：`CarLife/05_产物/CarLife4.0车机端个人修改版1.43_修兜底拆组.apk`（2,602,340 B，v1+v2+v3 签名）。静态门禁 201 方法 0 冲突；dex 含新日志串 `disarmed: keepP2pGroup=true, P2P group kept`。**车机实机待验**。
- 会话 20:20:13 用户手切热点后全链路成功（鉴权→视频 60fps），说明兜底复用热点传输的路线本身没问题，唯一堵点就是拆组。

## BootTask v1.4 发布：静音真正压住车机媒体声道
- 用户反馈：开机任务能唤醒 CarLife 但静音无效。根因：mute 只调 `setRingerMode(SILENT)`，车机媒体走 STREAM_MUSIC 不吃铃音模式。
- 修：`ExecService.smali` mute 追加 `setStreamVolume(STREAM_MUSIC,0,0)` + `setStreamMute(STREAM_MUSIC,true)`；unmute 对称恢复（流级静音取消 + 音量回最大值一半）。versionCode 5 / 1.4。
- build.sh 补拷 `NetWatch.java`（1.30 起共享 LogDownloadActivity 引用它，源码漂移导致 javac 失败）。
- 成品：`BootTask/dist/BootTask_v1.4.apk`（纯 v1 签名，sha256 cedd92…81ca）。

## 工具链迁移（重要环境变化）
- 老工具链在 C:/PJGG/apk 的机器上；本机（D:/PJGG/apk）已补齐：tools/jdk-17.0.20.1+1（Temurin JDK 17）、tools/apktool.jar(2.9.3)、tools/uber-apk-signer.jar、logxfer_src/tools/{android.jar(android-19), r8.jar(8.5.35)}、tools/bin/zip+zip.py（python 仿真 zip，支持 -q -d / -q -0，保留完整条目路径）。
- `03_构建脚本/build.sh` 与 `清理构建目录.py` 的 C 盘硬编码已改为 D 盘自推导。构建用法：`PRJ="D:/PJGG/apk/CarLife/01_工程源码/main" NAME="xxx.apk" bash 03_构建脚本/build.sh`。
- BootTask 构建需 PATH 里含 JDK bin + tools/bin（zip shim）+ `mkdir -p /tmp/opencode`。

## 待办 / 遗留
1. **车机实机验收 1.43**：判死→兜底挂载→日志应出现 `disarmed: keepP2pGroup=true, P2P group kept`→90s 轮询应打出 DIRECT- 组名+密码→手机手动入组→UDP 7999 广播→会话。
2. git 仓库被迁移污染：~2.2 万文件被暂存为删除（文件实际都在），且 `.git/index.lock` 被宿主进程占用无法 `git reset`。需关闭占用进程后 `git reset` 再提交 1.43 + 打 tag。

## 1.44 扫码入组（21:30 会话）
- 用户明确放弃修车机蓝牙（MCU 蓝牙≠Android BT 的误解已当面解释），选路线 A。
- 新增 QrFallback/QrFallbackActivity（logxfer 管线 Java→D8→smali）+ assets/logxfer/qr_wifi.html（复用 qrcode.js，WIFI: 标准格式含转义）。
- 注入点① NoBtFallback.showCredentials() :goto_1 处 show(ssid,psk)；② m/m/b.b() :cond_4 处 onLinkUp() 自动关码。
- 坑：Sable android-19 jar 残缺（无 JavascriptInterface），换 Robolectric android-all 4.4_r1-robolectric-1（Maven Central）；Google 已下架 platform-19 zip。
- 坑：apktool 3.0.3 dexlib 后端字符串池做后缀共享，朴素字节 grep 会误报 MISSING，须解析 string_ids 或反编译回读验证。
- 坑：Edit 工具对含 \uXXXX 转义的 smali 匹配不上（JSON 转义吃反斜杠），用 python chr(92) 构造锚点注入。
- 验证：构建 versionCode 144/mod1.44 签名 v1+v2+v3；成品反编译回读确认两处 invoke + 4 新类；门禁合并点 0 冲突。

## 21:38 五问分析（基于 carlife-logs-3 复核）
- Q1 扫码：P2P 持久组密码不变，手机保存网络后自动回连，不需每次扫；码页每次兜底激活都弹但 onLinkUp 自动关。
- Q2 手动配对不投屏实锤：20:22:43 组建成功"等待手机发起连接"→20:22:44 组翻 false+cancelConnect→全程无手机广播/连接。手机端直连模式等蓝牙凭据不会主动；兜底 UDP 7999 要等 45s 判死才挂，20:22 会话 11s 就断。
- Q3 强开蓝牙实锤：每秒 enable() 返回 true（假成功），state 恒 10 从无 TURNING_ON；bluetooth_on=1 但实际关；写全局开关无 WRITE_SECURE_SETTINGS 权限；MAC/name=null → 栈/HAL/firmware 层坏，App 层已穷尽。
- Q4 关机：日志零电源代码，进程戛然而止（20:22 会话 11s 断）= MCU 级断电（ACC/低压/温保），非 App。
- 20:20 热点会话仍为唯一 100% 验证路径。
