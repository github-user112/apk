# 2026-09-25

## 车机实机 1.44 首次全流程成功（日志分析）
来源：`C:/Users/gpj/Downloads/carlife-logs.zip`（3 个会话）。手机 Redmi 23113RKC6C（vermeer, Android 16, CarLife 8.9.0）。

- **★1.44 无蓝牙直连兜底首次在车机实机跑通**：会话2 09:04:07 `connect packet:192.168.49.72` → 鉴权 authen_result:true → onConnectionEstablished → 会话持续到 09:04:58 手机主动断开（ECONNRESET，正常）。二维码上屏→手机扫码入组→自动建链全链路 OK。
- **慢的根因（时间轴，会话2）**：09:02:18 启动 → 蓝牙死等 45s（09:03:05 判死）→ 兜底挂载 +5s 等 IP 超时 → 09:03:10 二维码上屏 → 09:04:07 手机广播 → 秒连 → 09:04:08 视频首帧 → 09:04:14 达 60fps/1Mbps。启动到流畅投屏共 116s：**车机侧串行浪费 52s（45s 蓝牙死等 + 5s IP 超时 + ~2s 初始化）≈45%**；二维码→广播隔 57s 为手机侧（入网+开 App，车机兜底挂载后已 terminate P2P receiver，看不到手机入网时刻，无法进一步归因）。
- **车机管线不慢的实锤**：会话1 兜底挂载当秒（08:57:09）就收到手机广播→1s 内鉴权建链；会话2 09:04:07 广播→09:04:08 视频首帧→09:04:14 满帧。车机收广播→建链→视频全程 <1s+8s 起坡。bitrate 每 ~5s 从 ~590k 落到 ~167k 是 I 帧 GOP 周期（I_FRAME_INTERVAL=300），正常。
- **★新发现车机侧 bug：`awaitLocalIp` 等网卡 IP 5s 必超时**——车机明明是 GO 持有 192.168.49.1（p2p-p2p0-2），但热点传输的 localIps 枚举看不见 P2P 接口（可能只认 wlan0/eth0），两个会话都白等 5s 串行才出码。修法：枚举白名单加 p2p 接口/192.168.49.x 网段。
- BtGuard 诊断实锤：state 恒 10(OFF)、enable()=true 无效果、bluetooth_on=1、已配对=0 —— 4.4.2 车机 Android 蓝牙彻底死，45s 自愈等待纯浪费。
- **UI 缺陷**：MainFragment 30s 超时（09:02:49「连接超时，请重新连接」）比兜底就绪（52s）还早触发，误导用户。
- **优化方向（待做 1.45）**：① BtGuard 快速判死（state 恒 10 连续 ~5s 即判死，或 sDead 持久化到磁盘冷启动直跳兜底）② 兜底与蓝牙握手并行：P2P 组已就绪就先出二维码，蓝牙起来了再撤 ③ MainFragment 超时轴对齐兜底（>60s 或兜底触发后重置）。
- 会话3 09:07:10 日志只到 09:07:24（用户在蓝牙等待中途就下载了日志，正常）。

## 1.45 交付（首页角标+快速兜底）
- git：先补提交 1.43+1.44 为基线（3b975459，tag v1.44；此前仓库停在 v1.42）再叠 1.45。仓库级 git identity 配置为 github-user112（noreply 邮箱）；remote=git@github.com:github-user112/apk.git。
- 改动 4 项（详见 `04_文档/1.45_首页角标与快速兜底.md`）：QrBadge 首页角标（新类+qr_badge.html，不跳新页面）、BtGuard 6s 快速判死（sSawTransition 闸门）、★ConnLog.hasUsableLocalIp() 极性修复（if-nez→if-eqz，awaitLocalIp 白烧 5s 的真凶）、首页超时 30s→120s（s.smali）。
- 坑：QrBadge.Bridge 必须静态内部类；编译 android.jar 必须指到 android-all.jar（脚本已支持 ANDROID_JAR env）。
- 验证：门禁 202 方法 0 冲突；构建 versionCode 145/mod1.45 签名 v1+v2+v3；条目级 688→689 仅多 qr_badge.html；dex 字符串（QrBadge/sSawTransition/快速判死/首页角标/set timeout: 120000）全 OK。顺手修 strings.xml app_name（1.43/1.44 忘改，停在 1.42）。
- 成品：`05_产物/CarLife4.0车机端个人修改版1.45_首页角标与快速兜底.apk`（2,630,949 B）。MuMu/车机实机待验。
- AGENTS.md/CLAUDE.md 已同步 1.45；版本表加行。git tag v1.45 已打并 push。

## 1.45 车机实机下午复测：连上后手机停流 → 看门狗断链 → UDP 监听死循环（日志分析）
来源：`carlife-logs (3).zip`（同手机 Redmi 23113RKC6C / CarLife 8.9.0，mod1.45）。
- **1.45 改动实机生效**：蓝牙 6s 快速判死（17:19:23→17:19:29）、hasUsableLocalIp 极性修复生效、首页超时 120s（无再误报）、凭据 17:19:39 上屏 → 17:19:42 手机入组 → 17:19:59 广播秒连 → 17:20:05 鉴权成功+视频首帧。扫码入组链路本身 OK。
- **★P0 新 bug：断链后 UDP 7999 监听死循环**。17:20:56 `ConnectionEstablishHandler heartbeat timeout` 拆链后，`m/m/e/a` 线程每秒 `start read broadcast socket`→`SocketException: Socket is closed` 无限循环（17:20:56→17:23:38+），不再监听手机广播 → 一次断链后无蓝牙兜底永久失效，只能重启 App。修法：socket closed 后重建/退避重绑，或 catch 后退出线程由外层重建。
- 断链根因在手机侧：视频数据 17:20:05→17:20:50 持续到达解码（~9fps，上午同机 18-22fps，昨晚 1.44 时 60fps），17:20:50 起手机端 TCP 完全静默（连 channel1 心跳应答也没有），5s 后车机看门狗判死——看门狗行为正常。手机为何停流需手机端日志/复现观察（息屏？App 后台？）。
## 手机端 xlog 对齐验证（17:57 用户给 `carlife_20260925.xlog`）
- xlog=腾讯 Mars v2 格式，无 AES 密钥但**未加密只压缩**：从偏移 74 起暴力扫描 raw-deflate（`zlib.decompressobj(-15)`，要求 d.eof 且 printable>0.55），逐包推进解出 188 包/14K 字符。存档 `_tmp_logs/手机端xlog解码_20260925.log`（info 级、有乱码段但关键行全可读）。
- **双向对齐实锤（17:19 会话，手机 pid 31150）**：手机 17:20:04.97 `VIDEO_ENCODER_INIT_DONE [576:1024] frameRate=60` → 17:20:05.26 `MSG_CMD_VIDEO_ENCODER_START` ↔ 车机 17:20:05 首帧，秒级吻合；手机 17:20:56.001 收 `MSG_CONNECT_STATUS_DISCONNECTED` → stop VideoOutputThread + releaseVideoEncoder ↔ 车机 17:20:56 心跳超时——**断链是车机看门狗先动手，手机是被动的**（手机 TCP 17:20:50 起静默 5s 后车机才动手）。
- **手机断开后一直在重试**：17:21:27→17:22:42 `enableWifiConnect` false/true 反复切换，但再无 `VIDEO_ENCODER_START` → 与车机 UDP 死循环（收不到广播）互为印证，1.46 修的就是这一环。
- **仍未知**：手机 17:20:08→17:20:55 之间 info 级零日志，编码器没报错也没停——视频 17:20:50 断流最可能是 P2P/TCP 链路层停顿（车机 17:19:31-38 组弹跳 p2p-p2p0-0→1 后链路质量存疑），需 verbose 级手机日志/复现确认。上午 10:09 会话手机编码器 10:09:53 就 START 而车机 10:10:19 才收 connect packet（隔 26s），两次握手痕迹（"hake hands"），连接路径比想象的绕。

## 1.46 交付（修断链后 UDP 7999 死循环）
- 根因代码级确认：`m/m/e/a.smali` `a()V` 的 `if-nez v6, :cond_1`（e 非空即复用）+ `f()V` terminate 只 close 不置 null → 重连后线程在死 socket 上 receive 立抛 → catch 路径 sleep 1s → 死循环。
- 修复（单树 main 直接改，git bb43bba5，tag v1.46）：`a()V` 建 socket 前加 `isClosed()` 判别（null 或已关闭都重建），重建时打新 ConnLog 零参静态方法 `logHotspotRebind()`（"♻ UDP 7999 旧socket已关闭, 重建监听(1.46)"）。f() 未动（最小改动）。
- versionCode 146 / mod1.46，app_name 百度CarLife 1.46。成品 `05_产物/CarLife4.0车机端个人修改版1.46_断链重建UDP监听.apk`。
- 验证：门禁 203 方法 0 冲突；条目级 689=689 无增减（大小恰好同 1.45，勿只看大小——dex 字符串 isClosed/logHotspotRebind/重建监听(1.46) 已核对全在）；签名 v1+v2+v3。AGENTS/CLAUDE 已同步（ca2439dc）。**实机待验**：断链后应出 ♻ 行且再无 Socket is closed 连发。

## BootTask v1.5 开机静音守卫（车机日志 `boottask-logs.zip` 分析后交付）
- **★硬发现：apktool 打包 versionCode/versionName 以 AndroidManifest.xml 属性为准**，改 apktool.yml 的 versionInfo 无效。v1.4 只改了 yml（5/1.4），manifest 仍 4/1.3 → 车机诊断 `version=1.3 (4)`，差点误判没装上。改版本必须两边一起改。
- 车机实锤：17:08 开机 `muted: ringer SILENT + STREAM_MUSIC vol=0 + streamMute` 打出来了（没抛异常），但听感没静；同轮 `launched com.desay_svautomotive.carlife` 正常 → 规则引擎没问题，是静音本身。三种假设：① 太早被系统恢复 ② 被后续 App 抢回 ③ 不走 STREAM_MUSIC。
- v1.5 三招齐下（新增 Java 类 `BootTask/src/com/boottask/MuteGuard.java`，ExecService.smali 的 mute/unmute 分支改为一行 invoke-static 委托）：六流全静音(MUSIC/RING/NOTIFICATION/SYSTEM/ALARM/DTMF) + t+0.8/1.5/3/6/12/25/50/100/175s 延时重施 + `VOLUME_CHANGED_ACTION` 看门狗（3min 窗口，1.5s 自触发抑制）+ 每次回读 `music=0/15 ring=0/7 ... ringer=0 mute=true` 打日志 → **下次开机日志可直接定性是哪一种**。
- ★坑：`AudioManager.setStreamMute` 按调用方 Binder 计数，重复 mute(true) N 次要 unmute(false) N 次才解除 → 重施只清音量，**streamMute 一次会话只做一次**（代码里 sStreamMuteApplied 闸门）。
- 顺带：补 `MODIFY_AUDIO_SETTINGS` 权限（原来根本没声明）；unmute 改为还原静音前记录的各流原值（原来一律 max/2 会把用户音量顶高）；桌面图标名 `开机任务 1.5`，由 build.sh 从 manifest versionName 自动推导。
- 验证：成品 dex 含 MuteGuard/MuteGuard$1/$2/$2$1 + ExecService 两处 invoke-static 已改；反编译 versionCode 6 / versionName 1.5 / MODIFY_AUDIO_SETTINGS 在 / app_name 开机任务 1.5。**车机实测待做**（adb 无设备）。

## 车机声音问题诊断（18:31 用户给 carlife_20260925(1).xlog）
- ★该 xlog 实为**手机端**日志（com.baidu.carlife 8.6.6 / 小米 / 栈里 loopOnce=Android12），非车机端（车机包 desay_svautomotive.carlife 不在其中）。解码存档 `_tmp_logs/hu_20260925_1830.log`（1730 块 7.5MB，官方 v2 结构：magic0x09+len@5+str@9+payload@9+str_len+1，raw inflate 直接解）。
- 结论①媒体无声=**手机全天从未发媒体音频**：559 个音频包全为 streamType:2(VOICE_TTS,16k 单声道,2560B/包)，Media socket 连着但 streamType:1/media(44.1k 立体声) 0 次。CarLife 协议只转发投屏内音乐 App 的音频，手机自己放歌不走 CarLife——需确认播放方式。
- 结论②导航声大且不受控=疑似车机端 TTS 走 STREAM_NAVIGATION 流（volume 键只调媒体流；telechips HAL 导航流常固定满量）。待车机 dumpsys audio/SP `AUDIO_TRACK_STREAM_TYPE` 确认。
- 连带发现：BootTask MuteGuard v1.5 六流清单**漏了 NAVIGATION(6)/VOICE_CALL(0)** → 也解释了 9-24「车机静音无效」观察的一半。若 TTS 确认在导航流，补静音或把 TTS 改回媒体流（patch l/b.c 默认 或写 SP）。

## Skill 沉淀（20:25）
- xlog 解码能力已提成用户级 skill `mars-xlog-decode`（`~/.workbuddy/skills/mars-xlog-decode/`：SKILL.md + scripts/decode_xlog.py）。脚本支持 0x03-0x0D 全 magic 家族（0x09/0x07 deflate 直解、0x08 明文、0x0B/0x0D zstd、0x03-0x06 加密跳过计数），校验「payload 尾字节=0x00」。实测本次文件 1740 块（含 1730 真块+校验余量），与手工版一致。下次解析 .xlog 直接 `python <skill>/scripts/decode_xlog.py 文件` 即可。
