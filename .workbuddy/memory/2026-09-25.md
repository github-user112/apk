# 2026-09-25

## 车机实机 1.44 首次全流程成功（日志分析）
来源：`C:/Users/gpj/Downloads/carlife-logs.zip`（3 个会话）。手机 Redmi 23113RKC6C（vermeer, Android 16, CarLife 8.9.0）。

- **★1.44 无蓝牙直连兜底首次在车机实机跑通**：会话2 09:04:07 `connect packet:192.168.49.72` → 鉴权 authen_result:true → onConnectionEstablished → 会话持续到 09:04:58 手机主动断开（ECONNRESET，正常）。二维码上屏→手机扫码入组→自动建链全链路 OK。
- **慢的根因（时间轴，会话2）**：09:02:18 启动 → 蓝牙死等 45s（09:03:05 判死）→ 兜底挂载 +5s 等 IP 超时 → 09:03:10 二维码上屏 → 09:04:07 手机广播 → 秒连 → 09:04:08 视频首帧 → 09:04:14 达 60fps/1Mbps。启动到流畅投屏共 116s：**车机侧串行浪费 52s（45s 蓝牙死等 + 5s IP 超时 + ~2s 初始化）≈45%**；二维码→广播隔 57s 为手机侧（入网+开 App，车机兜底挂载后已 terminate P2P receiver，看不到手机入网时刻，无法进一步归因）。
- **车机管线不慢的实锤**：会话1 兜底挂载当秒（08:57:09）就收到手机广播→1s 内鉴权建链；会话2 09:04:07 广播→09:04:08 视频首帧→09:04:14 满帧。车机收广播→建链→视频全程 <1s+8s 起坡。bitrate 每 ~5s 从 ~590k 落到 ~167k 是 I 帧 GOP 周期（I_FRAME_INTERVAL=300），正常。
- **★新发现车机侧 bug：`awaitLocalIp` 等网卡 IP 5s 必超时**——车机明明是 GO 持有 192.168.49.1（p2p-p2p0-2），但热点传输的 localIps 枚举看不见 P2P 接口（可能只认 wlan0/eth0），两个会话都白等 5s 串行才出码。修法：枚举白名单加 p2p 接口/192.168.49.x 网段。
- BtGuard 诊断实锤：state 恒 10(OFF)、enable()=true 无效果、bluetooth_on=1、已配对=0 —— 4.4.2 车机 Android 蓝牙彻底死，45s 自愈等待纯浪费。
- **UI 缺陷**：MainFragment 30s 超时（09:02:49「连接超时，请重新连接」）比兜底就绪（52s）还早触发，误导用户。
- **优化方向（待做 1.45）**：① BtGuard 快速判死（state 恒 10 连续 ~5s 即判死，或 sDead 持久化到磁盘冷启动直跳兜底）② 兜底与蓝牙握手并行：P2P 组已就绪就先出二维码，蓝牙起来了再撤 ③ MainFragment 超时轴对齐兜底（>60s 或兜底触发后重置）。
- 会话3 09:07:10 日志只到 09:07:24（用户在蓝牙等待中途就下载了日志，正常）。
