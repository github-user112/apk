# 1.45 首页角标与快速兜底

> 2026-09-25。依据：车机 1.44 实机日志三会话（08:56 / 09:02 / 09:07，
> `C:/Users/gpj/Downloads/carlife-logs.zip`）分析。
> 1.44 全流程首次跑通（09:04:07 建链 → 09:04:14 满 60fps），但启动到二维码上屏 52s、
> 扫码后到建链期间还遭 30s 假「连接超时」误报。用户两条需求：
> ① 二维码不要跳新页面，在首页找个位置显示个小码即可；② 修掉等待时间。

## 改动清单

| # | 改动 | 文件 | 说明 |
|---|---|---|---|
| A | 二维码改首页小角标 | `logxfer_src/java/.../QrBadge.java`（新增）+ `assets/logxfer/qr_badge.html`（新增）+ `QrFallback.java` + `LogXferEntry.java` | 120dp WebView 角标叠在 frag_main 根布局上；优先放「直连/热点/有线」页签(conn_mode_bar=0x7f090140)右侧空带（顶栏下 112dp），窄屏退化为贴右上角；点角标任意处关闭；`onLinkUp()` 建链自动关；`LogXferEntry.bind(root)` 现在用 WeakReference 记住 frag_main 根 View 供角标挂载；**首页根不可用时仍退回 1.44 全屏 QrFallbackActivity**（Manifest 注册保留） |
| B | BtGuard 快速判死 | `logxfer_src/java/.../BtGuard.java` | state 恒 10(OFF) 且从未见过其它状态（`sSawTransition`）时，6s（`FAST_WAIT=6` 轮）即判死进兜底；见过 TURNING_ON 等任何迁移则回退老的 45s 自愈（realme ColorOS 慢 binder 场景行为不变）。poke(Settings.Global) 增加 sWait==4 一轮。判死文案：`连续 6s state=10(OFF) 且从未进入 TURNING_ON → 快速判死(1.45)` |
| C | 修 1.25 极性写反 ★ | `main/smali/com/baidu/carlifevehicle/ConnLog.smali` `hasUsableLocalIp()` | `check-cast` 后的 `if-nez v3, :goto_0` 写反 = **非空网卡直接跳回外层循环，任何网卡都没被检查过**，恒返回 false → `awaitLocalIp` 每次必烧满 5s（车机日志「⚠ 等待网卡 IP 超时(5s)」两次会话都出现，车机明明是 GO 持有 192.168.49.1）。改 `if-eqz v3, :goto_0`（空才跳过）。修复后兜底挂载时 GO 网卡立即可见 → 5s 串行等待归零。这是「血泪清单 #4 分支极性」第 5 例（1.14/1.23/1.25/1.26 后又一只） |
| D | 首页假超时对齐 | `main/smali/a/a/b/n/s.smali` | `Z`(连接超时) 0x7530(30s) → 0x1d4c0(120s)：兜底二维码 ~7s 上屏后手机扫码入组往往超 30s，30s 档在码还在屏上时就误报「连接超时，请重新连接」（1.44 日志 09:02:49 实锤）。q0/r0 两处调度共用 Z，一并受益 |

版本：versionCode 145 / versionName mod1.45 / app_name 百度CarLife 1.45（顺手修：1.43/1.44 忘了改 app_name，停在 1.42）。

## 时间轴收益（按 1.44 车机实测推算）

| 节点 | 1.44 | 1.45 |
|---|---|---|
| 启动 → 二维码上屏 | ~52s（45s 蓝牙死等 + 5s IP 超时 + 2s 初始化） | **~8s**（6s 快判死 + 2s 初始化） |
| 二维码形态 | 全屏新页面 | 首页 120dp 小角标，点码即关 |
| 30s 假「连接超时」 | 出现（误导） | 消除（120s） |

## 验证状态

- 静态门禁：`verify_dalvik_合并点检查.py main` 202 方法 0 冲突 ✅
- 构建复核：versionCode 145 / mod1.45，签名 v1+v2+v3 ✅
- 条目级对比（vs 1.44）：688→689，仅多 `assets/logxfer/qr_badge.html`，零缺失 ✅
- dex 字符串：`QrBadge` / `sSawTransition` / `快速判死` / `首页角标` / `set timeout: 120000` 全 OK ✅
- **MuMu / 车机实机待验**（按惯例，Dalvik 过 ≠ 真机过；新类 QrBadge 需 ART 起一遍确认 0 VerifyError）
- 车机验收锚点：日志应出现 `快速判死(1.45)`（启动后 ~6s）、`✔ 网卡 IP 已就绪`（不再超时）、
  `二维码已上屏(首页角标...)`；屏上页签右侧出现小二维码，手机扫码 → 建链 → 角标自动消失。
